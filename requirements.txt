# Core ML  (install_requires in setup.py lists these — keep in sync)
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.0

# Data processing
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
pydicom>=2.4.0

# Evaluation
scikit-learn>=1.3.0

# Deployment
onnx>=1.14.0
onnxruntime>=1.15.0

# Optional: graph-optimisation pass for ONNX export
# onnxoptimizer>=0.3.0

# Training utilities
tqdm>=4.65.0
wandb>=0.15.0

# Visualisation (used in notebooks and evaluate.py plots)
matplotlib>=3.7.0

# ── Dev / test ────────────────────────────────────────────────────────────
# Install with:  pip install -e ".[dev]"
# pytest, black, flake8, mypy, isort are declared in setup.py [dev] extra
